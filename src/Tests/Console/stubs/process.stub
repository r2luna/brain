<?php

test('check list of tasks', function (): void {
    $process = new App\Brain\Domain\ExampleProcess();

    expect($process->getTasks())
        ->toBe([
            // List of the tasks
            // App\Brain\Domain\ExampleTask::class,
        ]);
});
