<?php

test('check list of tasks', function (): void {
    $process = new {{ namespace }}\{{ class }}();

    expect($process->getTasks())
        ->toBe([
            // List of the tasks
        ]);
});
